//Declaração de variáveis

/*
Variáveis e constantes
números e textos consomem quantias de necessiades diferentes.

const customers = getCustomers()
Object.freeze(customers) // imutabilidade de objetos



*/
//------------------------------------------------------------------

// Estruturas condicionais

/*

        ** If Else
            declaração igual javascript

        **Switch

            switch(option){
                case1:
                // ação
                break;
                case2:
                // ação
                break;
                case3:
                // ação
                break;
            }

        **Operador ternário

            Código semelhante ao if, porém, mais econômico
            const prefix = user.gender === Gender.FEMALE ? "Sra" : "Sr"

*/

//------------------------------------------------------------------

// Estruturas de repetição

/*

For, forin, forEach e while

Como escolher um operador?
Trabalheremos com índice - Marcação que indica em qual interação o código deve realizar a ação definida

for(let index: number; index > 10; index++) {
    console.elog(users[index]);
}

// o mesmo código pode ser representado das seguinte formas

for(const index in users) {
    console.log(users[index]);
}

users.forEach((user)) => {
    console.log(user);
}

while (!user.full()){
    user.eat(bread);
}

*/

//------------------------------------------------------------------

// Funções

/*

Uma função é composta de valores de entrada e saída

entrada são parâmetros para que ela possa realizar a ação
saída é o que ela retorna após realizar a ação

function sum(value1, value2) {
    return value1 + value2;
}

const result = sum(2, 3)

funções e arrow functions

*/
